# Superseded: full-pipeline Parquet engine benchmark

This path is preserved for existing links. The benchmark run originally published here was invalidated because a separate concurrent external Claude benchmark process appended to the same results file.

None of that run's single-repetition numbers, analysis, or recommendations are accepted. Use the validated three-repetition report instead: [Full-pipeline Parquet benchmark: DuckDB vs @dsnp/parquetjs](./2026-07-17-parquet-pipeline-bench.md).
