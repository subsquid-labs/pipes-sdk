{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "tasks": {
    "@sqd-pipes/pipes#build": {
      "inputs": [
        "src/**/*.js",
        "scripts/**/*.js",
        "package.json",
        "README.md",
        "../README.md",
        "tsconfig.json",
        "tsconfig.*.json",
        "tsup.config.js",
        "../tsconfig.json"
      ],
      "outputs": ["dist/**", "dist-dts/**"],
      "outputLogs": "new-only"
    },
    "pack": {
      "dependsOn": ["build"],
      "inputs": ["dist/**"],
      "outputs": ["package.tgz"],
      "outputLogs": "new-only"
    }
  }
}
